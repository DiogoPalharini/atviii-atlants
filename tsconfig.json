{
  "compilerOptions": {
    /* JavaScript e Linguagem */
    "target": "es2016",                              /* Define a versão do JavaScript gerado. */
    "module": "commonjs",                            /* Usa o sistema de módulos CommonJS. */
    "outDir": "./dist",                              /* Define a pasta de saída dos arquivos compilados. */
    "rootDir": "./src",                              /* Define a pasta raiz dos arquivos TypeScript. */

    /* Configurações de Importação e Exportação */
    "esModuleInterop": true,                         /* Facilita a importação de módulos CommonJS. */
    "allowSyntheticDefaultImports": true,            /* Permite usar `import x from y` mesmo que o módulo não tenha uma exportação padrão. */

    /* Checagem de Tipos */
    "strict": true,                                  /* Habilita todas as verificações estritas de tipo. */
    "forceConsistentCasingInFileNames": true,        /* Força o uso consistente de maiúsculas e minúsculas nos nomes dos arquivos. */
    "skipLibCheck": true                             /* Ignora a verificação de tipos em arquivos `.d.ts`. */
  },
  "include": [
    "src/**/*"                                       /* Inclui todos os arquivos dentro da pasta `src`. */
  ],
  "exclude": [
    "node_modules",                                  /* Exclui a pasta `node_modules` do processo de compilação. */
    "dist"                                          /* Exclui a pasta de saída para evitar conflitos. */
  ]
}
